'use client'; import {useEffect} from 'react'; import {AnimatePresence,motion} from 'framer-motion'; import {ChevronLeft,ChevronRight,X} from 'lucide-react';
export function ImageLightbox({images,index,onClose,onChange,label}:{images:string[];index:number|null;onClose:()=>void;onChange:(n:number)=>void;label:string}){useEffect(()=>{if(index===null)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();if(e.key==='ArrowLeft')onChange((index-1+images.length)%images.length);if(e.key==='ArrowRight')onChange((index+1)%images.length)};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[index,images.length,onClose,onChange]);return <AnimatePresence>{index!==null&&<motion.div role="dialog" aria-modal="true" aria-label={label} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={onClose} className="fixed inset-0 z-50 grid place-items-center bg-slate-950/95 p-4"><button aria-label="Close image" onClick={onClose} className="focusable absolute right-5 top-5 rounded-md p-3"><X/></button>{images.length>1&&<><button aria-label="Previous screenshot" onClick={e=>{e.stopPropagation();onChange((index-1+images.length)%images.length)}} className="focusable absolute left-2 rounded-md p-3 md:left-6"><ChevronLeft size={32}/></button><button aria-label="Next screenshot" onClick={e=>{e.stopPropagation();onChange((index+1)%images.length)}} className="focusable absolute right-2 rounded-md p-3 md:right-6"><ChevronRight size={32}/></button></>}<motion.img key={images[index]} initial={{scale:.97}} animate={{scale:1}} onMouseDown={e=>e.stopPropagation()} draggable={false} src={images[index]} alt={label} className="max-h-[84vh] max-w-[90vw] object-contain"/><span className="absolute bottom-5 text-sm text-slate-400">{index+1} / {images.length}</span></motion.div>}</AnimatePresence>}
