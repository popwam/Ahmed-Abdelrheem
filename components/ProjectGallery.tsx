'use client'; import {useState} from 'react'; import {motion} from 'framer-motion'; import {Maximize2} from 'lucide-react'; import {ImageLightbox} from './ImageLightbox';
export function ProjectGallery({images,title}:{images:string[];title:string}){const[index,setIndex]=useState<number|null>(null);return <section className="mt-12" aria-label="Project screenshots"><h2 className="text-2xl font-bold">Dashboard gallery</h2><div className="mt-5 grid gap-5 md:grid-cols-2">{images.map((src,i)=><motion.button whileHover={{y:-3}} className="focusable group relative overflow-hidden rounded-xl border border-white/10 bg-slate-950/40" onClick={()=>setIndex(i)} key={src} aria-label={`Open screenshot ${i+1}`}><img draggable={false} src={src} alt={`${title} screenshot ${i+1}`} className="aspect-video w-full object-contain transition duration-500 group-hover:scale-[1.02]"/><span className="absolute bottom-3 right-3 rounded bg-ink/80 p-2 text-cyan"><Maximize2 size={16}/></span></motion.button>)}</div><ImageLightbox images={images} index={index} onClose={()=>setIndex(null)} onChange={setIndex} label={title+' screenshot'}/></section>}
